// Who Am I Becoming? - Personal Transformation Journey\n\n// Initialize goals and reflections from localStorage\nlet goals = JSON.parse(localStorage.getItem('goals')) || [];\nlet reflections = JSON.parse(localStorage.getItem('reflections')) || [];\n\n// Transformation levels\nconst transformationLevels = {\n    'The Seeker': { min: 0, max: 5, emoji: '🌱' },\n    'The Grower': { min: 6, max: 15, emoji: '🌿' },\n    'The Builder': { min: 16, max: 30, emoji: '🌳' },\n    'The Visionary': { min: 31, max: Infinity, emoji: '🕉️' }\n};\n\n// DOM Elements\nconst modal = document.getElementById('goalModal');\nconst celebrationModal = document.getElementById('celebrationModal');\nconst addGoalBtn = document.getElementById('addGoalBtn');\nconst getStartedBtn = document.getElementById('getStartedBtn');\nconst closeBtn = document.querySelector('.close');\nconst closeCelebration = document.querySelector('.close-celebration');\nconst goalForm = document.getElementById('goalForm');\nconst goalsContainer = document.getElementById('goalsContainer');\nconst reflectionForm = document.getElementById('reflectionForm');\nconst reflectionsList = document.getElementById('reflectionsList');\nconst contactForm = document.getElementById('contactForm');\n\n// Event Listeners\naddGoalBtn.addEventListener('click', openModal);\ngetStartedBtn.addEventListener('click', openModal);\ncloseBtn.addEventListener('click', closeModal);\ncloseCelebration.addEventListener('click', closeModal);\ngoalForm.addEventListener('submit', addGoal);\nreflectionForm.addEventListener('submit', addReflection);\ncontactForm.addEventListener('submit', handleContactSubmit);\nwindow.addEventListener('click', outsideClickClose);\n\n// Modal Functions\nfunction openModal() {\n    modal.style.display = 'block';\n    goalForm.reset();\n}\n\nfunction closeModal() {\n    modal.style.display = 'none';\n    celebrationModal.style.display = 'none';\n}\n\nfunction outsideClickClose(event) {\n    if (event.target === modal) {\n        closeModal();\n    }\n    if (event.target === celebrationModal) {\n        closeModal();\n    }\n}\n\n// Get Transformation Level\nfunction getTransformationLevel(completedGoals) {\n    for (const [level, range] of Object.entries(transformationLevels)) {\n        if (completedGoals >= range.min && completedGoals <= range.max) {\n            return level;\n        }\n    }\n    return 'The Seeker';\n}\n\n// Get Transformation Message\nfunction getTransformationMessage(completedGoals) {\n    const level = getTransformationLevel(completedGoals);\n    const messages = {\n        'The Seeker': '🌱 You are awakening to your potential and discovering your path',\n        'The Grower': '🌿 You are growing with courage and embracing transformation',\n        'The Builder': '🌳 You are building your legacy and creating lasting impact',\n        'The Visionary': '🕉️ You are a visionary living your fullest potential'\n    };\n    return messages[level] || messages['The Seeker'];\n}\n\n// Add Goal Function\nfunction addGoal(e) {\n    e.preventDefault();\n\n    const goal = {\n        id: Date.now(),\n        title: document.getElementById('goalTitle').value,\n        description: document.getElementById('goalDescription').value,\n        category: document.getElementById('goalCategory').value,\n        deadline: document.getElementById('goalDeadline').value,\n        completed: false,\n        createdAt: new Date().toISOString()\n    };\n\n    goals.push(goal);\n    localStorage.setItem('goals', JSON.stringify(goals));\n    displayGoals();\n    updateStats();\n    closeModal();\n    showNotification('Goal added to your transformation journey! 🌸');\n}\n\n// Display Goals Function\nfunction displayGoals() {\n    goalsContainer.innerHTML = '';\n\n    if (goals.length === 0) {\n        goalsContainer.innerHTML = '<p style=\"color: #64748b; text-align: center; padding: 2rem;\">Start defining who you want to become. Add your first goal now!</p>';\n        return;\n    }\n\n    goals.forEach(goal => {\n        const goalElement = document.createElement('div');\n        goalElement.className = `goal-item ${goal.completed ? 'completed' : ''}`;\n        goalElement.style.opacity = goal.completed ? '0.7' : '1';\n\n        const daysLeft = calculateDaysLeft(goal.deadline);\n        const statusColor = goal.completed ? '#10b981' : daysLeft <= 7 ? '#f59e0b' : '#6366f1';\n\n        goalElement.innerHTML = `\n            <div class=\"goal-info\">\n                <h4 style=\"text-decoration: ${goal.completed ? 'line-through' : 'none'}\">${goal.title}</h4>\n                <p>${goal.description || 'No description'}</p>\n                <div style=\"display: flex; gap: 1rem; margin-top: 0.5rem; font-size: 0.85rem; flex-wrap: wrap;\">\n                    <span style=\"background: #e2e8f0; padding: 0.25rem 0.75rem; border-radius: 20px;\">${goal.category}</span>\n                    <span style=\"color: ${statusColor};\">\n                        ${goal.completed ? '✓ Completed' : `⏰ ${daysLeft} days left`}\n                    </span>\n                </div>\n            </div>\n            <div class=\"goal-actions\">\n                ${!goal.completed ? `<button class=\"complete-btn\" onclick=\"completeGoal(${goal.id})\">Complete</button>` : ''}\n                <button class=\"delete-btn\" onclick=\"deleteGoal(${goal.id})\">Delete</button>\n            </div>\n        `;\n\n        goalsContainer.appendChild(goalElement);\n    });\n}\n\n// Calculate Days Left\nfunction calculateDaysLeft(deadline) {\n    const today = new Date();\n    const deadlineDate = new Date(deadline);\n    const difference = deadlineDate - today;\n    const daysLeft = Math.ceil(difference / (1000 * 60 * 60 * 24));\n    return daysLeft > 0 ? daysLeft : 0;\n}\n\n// Complete Goal Function\nfunction completeGoal(id) {\n    const goal = goals.find(g => g.id === id);\n    if (goal) {\n        goal.completed = true;\n        localStorage.setItem('goals', JSON.stringify(goals));\n        \n        const completedCount = goals.filter(g => g.completed).length;\n        const newLevel = getTransformationLevel(completedCount);\n        \n        // Show celebration\n        showCelebration(completedCount, newLevel);\n        \n        displayGoals();\n        updateStats();\n    }\n}\n\n// Show Celebration Modal\nfunction showCelebration(completedCount, level) {\n    const titleEl = document.getElementById('celebrationTitle');\n    const textEl = document.getElementById('celebrationText');\n    \n    const celebrationMessages = {\n        'The Seeker': {\n            title: '🌱 Goal Achieved!',\n            text: 'You are awakening to your potential!'\n        },\n        'The Grower': {\n            title: '🌿 Wonderful Progress!',\n            text: 'You are growing stronger every day!'\n        },\n        'The Builder': {\n            title: '🌳 Building Your Legacy!',\n            text: 'Your impact is becoming real!'\n        },\n        'The Visionary': {\n            title: '🕉️ You Are A Visionary!',\n            text: 'You are living your fullest potential!'\n        }\n    };\n    \n    const message = celebrationMessages[level] || celebrationMessages['The Seeker'];\n    titleEl.textContent = message.title;\n    textEl.textContent = message.text;\n    \n    celebrationModal.style.display = 'block';\n    createCelebrationParticles();\n    \n    setTimeout(() => {\n        closeModal();\n    }, 3000);\n}\n\n// Create celebration particles\nfunction createCelebrationParticles() {\n    const particlesContainer = document.querySelector('.celebration-particles');\n    particlesContainer.innerHTML = '';\n    \n    const particles = ['🌸', '✨', '🌟', '💫', '🌺'];\n    \n    for (let i = 0; i < 10; i++) {\n        const particle = document.createElement('div');\n        particle.className = 'particle';\n        particle.textContent = particles[Math.floor(Math.random() * particles.length)];\n        particle.style.left = Math.random() * 100 + '%';\n        particle.style.animation = `fall ${1 + Math.random()}s ease-in forwards`;\n        particlesContainer.appendChild(particle);\n    }\n}\n\n// Delete Goal Function\nfunction deleteGoal(id) {\n    if (confirm('Remove this goal from your journey?')) {\n        goals = goals.filter(g => g.id !== id);\n        localStorage.setItem('goals', JSON.stringify(goals));\n        displayGoals();\n        updateStats();\n        showNotification('Goal removed from your journey.');\n    }\n}\n\n// Add Reflection\nfunction addReflection(e) {\n    e.preventDefault();\n    \n    const reflectionText = document.getElementById('reflectionText').value;\n    \n    if (reflectionText.trim()) {\n        const reflection = {\n            id: Date.now(),\n            text: reflectionText,\n            date: new Date().toLocaleString()\n        };\n        \n        reflections.unshift(reflection);\n        localStorage.setItem('reflections', JSON.stringify(reflections));\n        \n        document.getElementById('reflectionText').value = '';\n        displayReflections();\n        showNotification('Reflection saved to your journey 🌸');\n    }\n}\n\n// Display Reflections\nfunction displayReflections() {\n    reflectionsList.innerHTML = '';\n    \n    if (reflections.length === 0) {\n        reflectionsList.innerHTML = '<p style=\"color: #64748b; text-align: center; padding: 1rem;\">Start reflecting on your transformation...</p>';\n        return;\n    }\n    \n    reflections.forEach(reflection => {\n        const reflectionEl = document.createElement('div');\n        reflectionEl.className = 'reflection-item';\n        reflectionEl.innerHTML = `\n            <p>${reflection.text}</p>\n            <p class=\"reflection-date\">${reflection.date}</p>\n        `;\n        reflectionsList.appendChild(reflectionEl);\n    });\n}\n\n// Update Statistics\nfunction updateStats() {\n    const totalGoals = goals.length;\n    const completedGoals = goals.filter(g => g.completed).length;\n    const inProgressGoals = totalGoals - completedGoals;\n    \n    const transformLevel = getTransformationLevel(completedGoals);\n    const transformMessage = getTransformationMessage(completedGoals);\n\n    document.getElementById('totalGoals').textContent = totalGoals;\n    document.getElementById('completedGoals').textContent = completedGoals;\n    document.getElementById('inProgressGoals').textContent = inProgressGoals;\n    document.getElementById('transformLevel').textContent = transformLevel;\n    document.getElementById('purposeMessage').textContent = transformMessage;\n}\n\n// Handle Contact Form Submission\nfunction handleContactSubmit(e) {\n    e.preventDefault();\n    showNotification('Thank you for connecting! We\\'ll be in touch soon. 🌸');\n    contactForm.reset();\n}\n\n// Notification Function\nfunction showNotification(message) {\n    const notification = document.createElement('div');\n    notification.style.cssText = `\n        position: fixed;\n        top: 20px;\n        right: 20px;\n        background: linear-gradient(135deg, #6366f1 0%, #ec4899 100%);\n        color: white;\n        padding: 1rem 2rem;\n        border-radius: 8px;\n        box-shadow: 0 5px 20px rgba(0, 0, 0, 0.2);\n        z-index: 9999;\n        animation: slideInRight 0.3s ease;\n        max-width: 300px;\n    `;\n    notification.textContent = message;\n    document.body.appendChild(notification);\n\n    setTimeout(() => {\n        notification.style.animation = 'slideOutRight 0.3s ease';\n        setTimeout(() => {\n            notification.remove();\n        }, 300);\n    }, 3000);\n}\n\n// Add animation styles\nconst style = document.createElement('style');\nstyle.textContent = `\n    @keyframes slideInRight {\n        from {\n            transform: translateX(400px);\n            opacity: 0;\n        }\n        to {\n            transform: translateX(0);\n            opacity: 1;\n        }\n    }\n    \n    @keyframes slideOutRight {\n        from {\n            transform: translateX(0);\n            opacity: 1;\n        }\n        to {\n            transform: translateX(400px);\n            opacity: 0;\n        }\n    }\n`;\ndocument.head.appendChild(style);\n\n// Initialize on page load\ndocument.addEventListener('DOMContentLoaded', () => {\n    displayGoals();\n    displayReflections();\n    updateStats();\n});\n